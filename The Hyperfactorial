import numpy

def hyperfact(num):
    numbers, numPlus, i = [], num + 1, 0
    while i < num:
        numPlus = numPlus - 1
        numbers.append(pow(numPlus, numPlus))
        i += 1
        
    return (numpy.prod(numbers) % 1000000007)

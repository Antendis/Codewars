def hyperfact(num):
    numbers, numPlus, i = [], num + 1, 0
    while i < num:
        numPlus = numPlus - 1
        numbers.append(pow(numPlus, numPlus))
        i += 1
    print(numbers)
    answer = 1
    
    for num in numbers:
        answer *= num
    print(answer)
    
    return 
